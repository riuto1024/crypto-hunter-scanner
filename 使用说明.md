# 加密猎手 · 市场扫描器 V1（免费版）

这是一个中文网页版加密货币日内交易候选池扫描器。

第一层“市场质量池”使用 CoinGecko 的市值/全市场24H成交额，以及 Binance USDT 永续公开行情的永续成交额、24H振幅、盘口价差、资金费率。

第二层“Hunter技术候选池”只对市场质量排名靠前的币继续检查 4H 大方向、1H 趋势/回调/风险、15M 价值区。

最终 5M / 1M 精确进场仍交给 TradingView 的“加密猎手·精准交易”指标。

## Windows 本地启动

1. 安装 Python。
2. 解压文件夹。
3. 在该文件夹地址栏输入 `cmd` 回车。
4. 执行：`py -m pip install -r requirements.txt`
5. 执行：`py -m streamlit run app.py`
6. 浏览器会自动打开。

## 免费部署

将 `app.py` 和 `requirements.txt` 上传到 GitHub 仓库，再在 Streamlit Community Cloud 部署，入口文件选择 `app.py`。

## CoinGecko

默认使用免 Key 公共接口。若限流，可免费申请 Demo Key，在网页左侧填写，不需要修改代码。

## 注意

市场质量分不是胜率；技术机会分也不是立即买卖信号。V1 的评分只是初始规则，后续必须用真实统计结果校准。
